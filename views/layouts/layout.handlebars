<!DOCTYPE html>
<html>
<head>
    <title>Koordi '17</title>
    <link rel="stylesheet" href="/css/bootstrap.css" />
    <link rel="stylesheet" href="/css/style.css" />
    <link href="/vendor/font-awesome/css/font-awesome.min.css" rel="stylesheet" type="text/css">
</head>
<body>
<div class="container">
    <div class="header clearfix">
        <nav>
            <ul class="nav nav-pills pull-right">
                {{#if user}}
                    <li role="presentation"><a href="/users/details">Kezdőoldal</a></li>
                    <li role="presentation"><a href="/users/logout">Kijelentkezés</a></li>
                {{else}}
                    <!---<li role="presentation"><a href="/users/login">Login</a></li>--->
                {{/if}}
            </ul>
        </nav>
        <h3 class="text-muted">Koordi '17</h3>
    </div>


    <div class="row">
        <div class="col-lg-12">
            {{#if success_msg}}
                <div class="alert alert-success">{{success_msg}}</div>
            {{/if}}

            {{#if error_msg}}
                <div class="alert alert-danger">{{error_msg}}</div>
            {{/if}}

            {{#if error}}
                <div class="alert alert-danger">{{error}}</div>
            {{/if}}

            {{{body}}}
        </div>


    </div>

    <footer class="footer" style="size: 7px; ">
        <p >2016-2017 Magyar Balázs<br>magyarb94@gmail.com<br><br>Köszönet az ötletekért Csokinak, Kristófnak, Adrinak és Robinnak <3</p>
    </footer>

</div> <!-- /container -->
</body>
</html>
